# 2026-07-02 Daily Notes（已過時，部分內容已遷移至 SYSTEM-LESSONS.md）

> ⚠️ 此文件已過時。重要 lesson 已遷移至 `memory/SYSTEM-LESSONS.md`。
> SSH key direction lesson → SYSTEM-LESSONS.md ✅
> Bot status → MEMORY.md ✅
> SSH connections → MEMORY.md ✅

## SSH Key Path Issue (Important)

**Problem:** Kimi Boy (group bot) was using wrong SSH key path when connecting to Oracle VM from Zeabur VM.

- Kimi Boy tried: `~/.ssh/id_ed25519_zo_new` ❌ (this key doesn't exist on Zeabur VM)
- Correct key on Zeabur VM: `~/.ssh/oracle_vm_new` ✅
- SSH config at `~/.ssh/config` already has correct mapping

**Root cause:** The SSH backup document lists `~/.ssh/id_ed25519_zo_new` as the key for Oracle VM, but this is from the Oracle VM's perspective (the key lives on Oracle VM). When connecting FROM Zeabur TO Oracle, the Zeabur VM needs to use its own key (`oracle_vm_new`), not the Oracle VM's key.

## Bot Status
- arslansky bot: ✅ working
- know2learn bot: ✅ working
- Janzaibot (Big VM): ✅ working
- MiniMax timeout fix applied: `timeoutSeconds: 300` in catalog.json

## SSH Connections
- Zeabur VM (this machine) → Oracle VM (161.118.247.199): ✅ Working
  - Use: `ssh oracle-new` or `ssh opc@161.118.247.199`
  - Key: `~/.ssh/temp_opc_key`（舊 key `oracle_vm_new` 已廢棄 IP）
